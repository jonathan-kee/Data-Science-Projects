name: Pipeline

on:
  workflow_dispatch:

jobs:
  Pipeline:
    name: Setup Pipeline
    runs-on: [self-hosted, macOS, ARM64]
    defaults:
      run:
        working-directory: /Users/jonathankee/Data-Science-Projects
    steps:
      - name: Print working directory
        run: |
          pwd

      - name: Set current date
        run: echo "TODAY=$(date +'%d%m%Y')" >> $GITHUB_ENV

      - name: Use the date
        run: echo "Today's date is $TODAY"
      
      # ------------------------------------- Smeltor -------------------------------------
      - name: Exchange ticker AL.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/AL.AI1"

      - name: Run SQL Exchange ticker AL.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_AL_AI1_${TODAY}.sql

      - name: Exchange ticker AU.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/AU.AI1"

      - name: Run SQL Exchange ticker AU.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_AU_AI1_${TODAY}.sql
      
      - name: Exchange ticker CF.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/CF.AI1"

      - name: Run SQL Exchange ticker CF.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_CF_AI1_${TODAY}.sql

      - name: Exchange ticker CU.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/CU.AI1"

      - name: Run SQL Exchange ticker CU.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_CU_AI1_${TODAY}.sql
        
      - name: Exchange ticker FE.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/FE.AI1"

      - name: Run SQL Exchange ticker FE.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_FE_AI1_${TODAY}.sql
      
      - name: Exchange ticker LI.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/LI.AI1"

      - name: Run SQL Exchange ticker LI.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_LI_AI1_${TODAY}.sql

      - name: Exchange ticker S.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/S.AI1"

      - name: Run SQL Exchange ticker S.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_S_AI1_${TODAY}.sql

      - name: Exchange ticker STL.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/STL.AI1"

      - name: Run SQL Exchange ticker STL.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_STL_AI1_${TODAY}.sql

      - name: Exchange ticker TI.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/TI.AI1"

      - name: Run SQL Exchange ticker TI.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_TI_AI1_${TODAY}.sql
      
      - name: Exchange ticker SI.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/SI.AI1"

      - name: Run SQL Exchange ticker SI.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_SI_AI1_${TODAY}.sql

      - name: Exchange ticker RE.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/RE.AI1"

      - name: Run SQL Exchange ticker RE.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_RE_AI1_${TODAY}.sql
      # ------------------------------------- Smeltor -------------------------------------

      # ------------------------------------- Metalist Studio -------------------------------------
      - name: Exchange ticker SEQ.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/SEQ.AI1"

      - name: Run SQL Exchange ticker SEQ.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_SEQ_AI1_${TODAY}.sql

      - name: Exchange ticker BGO.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/BGO.AI1"

      - name: Run SQL Exchange ticker BGO.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_BGO_AI1_${TODAY}.sql

      - name: Exchange ticker MFK.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/MFK.AI1"

      - name: Run SQL Exchange ticker MFK.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_MFK_AI1_${TODAY}.sql

      - name: Exchange ticker BRO.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/BRO.AI1"

      - name: Run SQL Exchange ticker BRO.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_BRO_AI1_${TODAY}.sql

      - name: Exchange ticker BFR.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/BFR.AI1"

      - name: Run SQL Exchange ticker BFR.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_BFR_AI1_${TODAY}.sql

      - name: Exchange ticker RGO.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/RGO.AI1"

      - name: Run SQL Exchange ticker RGO.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_RGO_AI1_${TODAY}.sql

      - name: Exchange ticker UTS.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/UTS.AI1"

      - name: Run SQL Exchange ticker UTS.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_UTS_AI1_${TODAY}.sql

      - name: Exchange ticker BCO.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/BCO.AI1"

      - name: Run SQL Exchange ticker BCO.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_BCO_AI1_${TODAY}.sql

      - name: Exchange ticker AFR.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/AFR.AI1"

      - name: Run SQL Exchange ticker AFR.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_AFR_AI1_${TODAY}.sql

      - name: Exchange ticker SFK.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/SFK.AI1"

      - name: Run SQL Exchange ticker SFK.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_SFK_AI1_${TODAY}.sql

      - name: Exchange ticker HCC.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/HCC.AI1"

      - name: Run SQL Exchange ticker HCC.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_HCC_AI1_${TODAY}.sql

      - name: Exchange ticker BGC.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/BGC.AI1"

      - name: Run SQL Exchange ticker BGC.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_BGC_AI1_${TODAY}.sql
      
      - name: Exchange ticker FLO.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/FLO.AI1"

      - name: Run SQL Exchange ticker FLO.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_FLO_AI1_${TODAY}.sql
      # ------------------------------------- Metalist Studio -------------------------------------


      - name: Exchange ticker ALO.AI1
        run: tsc && node ./build/ingestion/json/cxpc.js "https://rest.fnar.net/exchange/cxpc/ALO.AI1"

      - name: Run SQL Exchange ticker ALO.AI1
        run: docker exec -i -e PGPASSWORD=abc123 postgres-container psql --dbname=prosperous_universe --username=postgres < ingestion/sql/cxpc_ALO_AI1_${TODAY}.sql

      - name: Building costs HB2
        run: tsc && node ./build/ingestion/json/building.js "https://rest.fnar.net/building/HB2"

      - name: Building costs FS
        run: tsc && node ./build/ingestion/json/building.js "https://rest.fnar.net/building/FS"
      
      - name: Prices
        run: python ingestion/csv/ingest.py "https://rest.fnar.net/csv/prices" 

      - name: Inventory
        run: python ingestion/csv/ingest.py "https://rest.fnar.net/csv/inventory?apikey=0f11ac24-ef14-428f-8213-4438576837f4&username=jonathan_kee"